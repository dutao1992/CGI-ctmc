# 验收记录 · 2026-08-26

## 实际交付

- 线上地址：<https://quality-ctmc.cloud/vehicle/>；质检主页第 05 个入口“车载数据服务”。
- 最终服务版本：`/srv/ctmc-vehicle/releases/20260826-160701`。
- 原平台发布路径未切换。车载数据使用独立库和进程；采集器只读原始文件。
- 当前真实设备 SN 6094510，已读取 28,184 条以上 GPCHCX 采样；该数字为 16:04 左右快照，仍在持续增长。后续浏览器已显示 29,340 条所选时段采样。

## 自动化

| 验收 | 结果 |
| --- | --- |
| 新车载服务 unittest，本机与线上 Python 3.11 | 21 项通过，0 跳过 |
| 原平台后台 unittest | 17 项通过 |
| 门户 npm test | 装配看板、部署配置、模块挂载测试通过 |
| 门户 npm run build | TypeScript 与 Vite 构建通过 |
| 新 JS 语法与生产资源指纹 | 通过；本地与生产 8 个代码/静态文件 SHA256 一致 |
| 新数据库 quick_check | ok |
| 原接收器 JS / ecosystem 指纹 | 全部与变更前相同，PID 保持 3234542 |
| 原账号回归 | 5 个账号原字段与原权限不变，仅管理员新增 vehicle 权限 |

新测试覆盖真实报文、坏校验拒绝、GPS 闰秒、十六进制状态/告警、分片和混合流、断点续读及去重、多设备隔离、断档和异常、峰值保留、规则质量门槛、CSRF/读写授权、CSV、路径越界以及原始文件变更时拒绝伪造证据。

## 线上浏览器

使用用户手动登录的真实管理员会话，从质检主页点击进入，并验证：

- 主页面显示 5/5 正常；新增入口和用户管理中的 vehicle 权限项存在。
- 真实设备显示在线、单点定位未定向、最新采样时间与位置精度。
- 地图正常加载 OSM 道路底图和真实轨迹；回放播放/暂停可切换。
- 15 分钟时段筛选正常；12 组惯导图表加载，浏览器无 error/warn。
- 事件筛选正常；设备告警可定位到 16:01:27 的 GPCHCX，显示“XOR 与原始字段复核通过”和真实源文件/字节偏移。
- 管理员配置入口可用；没有为验收擅自修改生产规则、确认安装或关闭真实事件。修改/处置成功路径在临时数据库测试。
- 390 px 手机视口实测文档宽度 390 px，无横向溢出。桌面与手机版截图在 `evidence/`。
- 最终页面加载指纹资源 `app.js?v=bdafcb5b8939`，已显示更新后的所选时段质量提示；HTML 设置 no-store。
- 未登录 `/vehicle/` 重定向登录，`/vehicle/api/devices` 返回 401。无模块权限和普通用户越权写入在自动化测试中返回 403。

## 设备与业务边界

当前样本为单点定位 / 未定向，RTK 固定解占比为 0%，横滚约 -90°；这是接收数据中的现状，不以系统名称中的“厘米级”替代实际质量。姿态安装未确认，相关业务预警默认关闭。

约 3,071 条较早的无 SN GPCHC 被隔离统计，未通过 IP 猜测归属。辅助 ASCII 和未知二进制保留原文件，未宣称全部二进制已解析。待续读字节包含未闭合尾部片段，不等于有效导航数据积压。

真实道路工况、车辆安装标定、RTK 初始化、可信设备鉴权、多车持续 100 Hz 容量与长期归档仍需要后续按实际部署条件验证。此版本不宣称符合性认证、法定违规认定或人体振动舒适度评级。

回滚方式见《运维与扩展.md》。服务器服务健康、接收原始数据及原业务健康已实测；不以测试替身或仅有进程 PID 代替线上验收。
