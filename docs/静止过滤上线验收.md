# 静止过滤上线验收 · 2026-08-26

发布：`/srv/ctmc-vehicle/releases/20260826-203604`。

## 已验证

- 本机和生产服务器 **42 项 Python + 5 项 Node 测试通过**。覆盖字段保留、无效航向、静止向量、初始化、±180°环形角差、设备/时间边界、未来真实运动、断档、CSV/原值分离、统一鉴权、分页、幂等回算、留存关联索引删除、实际地图及回放显示。
- 生产 SQLite 3.26.0：114,008 条采样全部判定；数值离群去重 7,384 条；待判定 0，孤儿过滤索引 0，`quick_check=ok`。
- 迁移前一致性备份：`/srv/ctmc-vehicle/backups/quality-before-20260826-203604.sqlite`。索引迁移约 16.3 秒，原始记录摘要前后完全相同：`10ebbdd33fead7ec93079e8a46d1fe02f182df99f23c148c15b292d221c211a5`。
- 生产全历史查询实际耗时约 6.7 秒；静止里程 0km、运动时间 0s，最高保留速度残差 1.08km/h。全部航向值为空，而健康 IMU 通道继续绘制。
- 在正式域名使用既有统一登录：全历史筛选显示 114,008 条；隔离台账显示 7,384 条，水平速度筛选显示 2,746 条，每页 50 条。核验 18:39:34.300 的异常采样：速度原值 0.45m/s 留在证据页，运行值为空，字节偏移 12377450 的原报文校验及字段复核通过。
- 12 张惯导图均呈现，附各通道剔除数量。手机 390px 下 documentWidth=390，无整页横向溢出；宽表独立横滑且有提示。保留正式数据质量页作为交付入口。
- 高德底图本次页面可见的 6 张瓦片全部加载，naturalWidth=256；静止地图不画漂移线，回放及坐标转换由 Node 实际应用测试覆盖。
- 9000 接收器仍为 PID 3234542，未重启；车载服务正常且 NRestarts=0，检查时内存约 49MB。发布后错误级日志无新增记录。
- 留存定时器 active；手动运行原生留存服务验证 Result=success / ExecMainStatus=0。80%/75%/24小时配置 SHA256 保持 `07512791ee737eedf95858d4dcad6dfc5c1645cdfb3281b413d407fe95740cef`，当前磁盘约38%，未触发清理。
- 生产与本地的 quality/store/server/rules/retention 及 app/index/style 八个运行文件 SHA256 完全一致。本地只读临时预览服务已停止，浏览器尺寸已恢复。

## 证据

本地 `evidence/filter-deploy.log`、`filter-tests.log`、`filter-production-verification.json`、`filter-query-benchmark.json`、`stationary-filter-validation.json` 和 `filter-quality-mobile.jpg`。这些运行证据不进入源码发布包；算法说明和重现入口在 `docs/静止数据过滤说明.md`、`analysis/静止数据审计.ipynb`。

## 边界

静止事实只绑定 SN6094510 已确认的 2026-08-26 15:08:55.100—18:39:57.900；未来/其他设备不套用静止阈值。统计离群不是已确认硬件故障，需查看原始证据。单点定位整体精度有限，中位数位置不是真值。原始报文仍受现有存储容量策略约束，隔离不是永久归档承诺。
