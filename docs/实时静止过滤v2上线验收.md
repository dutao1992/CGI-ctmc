# 实时静止过滤 v2 上线验收 · 2026-08-27

## 发布与迁移

- 正式版本：`/srv/ctmc-vehicle/releases/20260827-102755`。
- 迁移前一致性备份：`/srv/ctmc-vehicle/backups/quality-before-20260827-102755.sqlite`。
- 正式迁移 172,902 条，原始摘要 `fbc2fbbebab00f6897f731d3f9d185d601b8cc61c2e250faea8e1f3105919b06` 前后不变；v2 回算完成。
- 发布前先在生产数据库只读副本回放 169,807 条，原始摘要保持不变，`pending=0`、孤儿索引 0、`quick_check=ok`。

## 状态结论

- 2026-08-27 09:59:57 左右发生清晰配置切换：roll 由约 `-90.26°` 变为 `-0.25°`，重力由 X 轴约 1g 变为 Z 轴约 1g。安装角补偿已生效。
- 10:31 最新采样 `91`：卫导模式、RTK 浮点、未定向；warning `0x0002`。pitch/roll `0.42°/-0.26°`，速度 `0.02m/s`，水平定位标准差 `0.30/0.43m`。
- 尚未进入组合导航，RTK 固定解占比为 0%；航向因此仍按不可用字段隔离。算法不把浮点解宣传为厘米级固定解。

## 过滤验收

- 正式发布后新到 1,918 条，**1,918/1,918** 写入 quality v2，**1,918/1,918** 命中当前 SN 的持续静止上下文；`points=point_quality=174,843`，待判定 0、孤儿索引 0。
- 最近 15 分钟 8,493 条：4,894 条含数值异常字段；里程 `0km`、运动时间 `0s`、过滤后速度峰值 `1.08km/h`，有效位置占比约 50.61%。较多位置空缺由当前浮点定位不确定度超限造成，不是接收丢包。
- 新到异常样本 `1787797848.0`：原值 `speed=0.50m/s`、`vn=-0.50m/s`、heading `113.82°`；运行投影相应字段均为 null，原因是静止水平速度异常、未定向航向和低速航迹角不可用。源报文字节偏移复核通过，帧 SHA256 `7a218ea96ab0e842eb0dcc1ffff25b12ec20606061a6c0aa358bca6ccba7cb05`。
- 本地与服务器运行代码和前端 7 个文件 SHA256 完全一致。44 项 Python、10 项 Node 测试在本机及服务器均通过，0 skipped/todo。

## 运行边界

- 车载服务 active，NRestarts=0，内存约 39.5MB；发布后错误级日志无新增。
- TCP 9000 接收器保持 PID 3234542；`server.js` 与 `ecosystem.config.cjs` 指纹和历史基线一致。留存 timer active，80%/75%/24小时配置 SHA256 仍为 `07512791ee737eedf95858d4dcad6dfc5c1645cdfb3281b413d407fe95740cef`。
- 正式域名未登录时按预期 302 到统一登录。内置浏览器既有会话已过期，因此本回合没有把“登录后的页面截图”冒充已验收；后端运行投影、静态文件一致性、API测试与新入站链路均已实际验证。
- 10:32:37 CGI 客户端连接因 `read ETIMEDOUT` 断开；10:34:09 复核时 TCP 9000 仍监听、接收器及解析服务心跳正常，但没有已建立客户端连接，最后设备采样为 10:31:56.5。该事件发生在独立接收链路，现状应显示设备离线；需从 CGI 蜂窝网络/Client7 连接侧恢复。
- 设备开始移动或再次修改安装参数前，必须按《静止数据过滤说明》关闭持续静止上下文；否则算法会按已确认事实继续隔离真实运动。

证据文件：[v2-production-verification.json](../evidence/v2-production-verification.json)、[v2-latest-link-state.json](../evidence/v2-latest-link-state.json)。
