# 真实车辆运行恢复上线验收（2026-09-01）

## 结论

今天设备并未断采。原始 GPCHCX 从北京时间 10:20:10.5 起持续写入，但设备一直处于 `nav_mode=1`（卫导模式）；旧自动退出只接受 `nav_mode=2`（组合导航），所以活动静止上下文把真实速度、位置和运动统计继续当成静止异常。问题在状态生命周期判据，不在 TCP 接收、解析、图表或原始数据。

已将活动静止上下文按第一个可复核的车辆运动证据结束于 **10:38:29.599**。10:20:10.5—10:38:29.5 保留为已确认静止，之后恢复普通运动投影；没有删除任何原始报文或 `points` 采样。

## 证据与新算法

原始数据回放的第一个强证据窗为 10:38:29.6—10:38:39.6：101 点、持续 10 秒、速度 1.73—8.25m/s、净位移 46.58m、路径长度 51.329m、轨迹有效率 90.7%、速度积分距离 48.64m、净位移/速度积分为 0.958、最大水平定位标准差 4.89m，状态为卫导模式 + RTK 浮点未定向。该证据同时满足坐标、速度向量、时间连续性和定位质量，明显不是单点跳变或静止漂移。

自动退出现在有两条互不放宽的路径：

- 低速小推车仍只接受组合导航，速度≥0.15m/s、定位 σ≤1m、15秒/10点、净位移≥8m、轨迹有效率≥50%；
- 车辆路径允许卫导或组合导航，但只接受 RTK 固定/浮点，要求速度≥1m/s、定位 σ≤5m、10秒/20点、净位移≥25m、轨迹有效率≥65%，速度与水平速度向量一致，且坐标净位移为速度积分的65%—135%。

完整证据窗确认后，从候选起点恢复普通质量投影并重建相关聚合桶，不再丢失行程开头 10—15 秒。低速卫导位移、单点跳变、定位降级、往返漂移和速度/位移不一致均不能触发。

## 今天曲线快照

截至 11:29:08.8，10:20 起共查询 41,382 条采样；静止结束后识别到多段行驶，估算里程 6.303km、有效运动 667.5秒、最高速度 67.14km/h、RTK 固定解占比 38.19%、有效定位占比 74.03%、采样率约 10Hz、时间断档 0。

已恢复的曲线及当前范围：速度 0—18.65m/s；pitch -15.04—16.77°；roll -16.80—16.44°；gx/gy/gz 分别 -10.35—9.28、-9.13—9.53、-27.25—23.10°/s；ax/ay/az 分别 -0.2888—0.3342、-0.2809—0.2937、0.5016—1.7622g；ve/vn/vu 分别 -17.73—12.88、-15.65—11.82、-6.29—6.01m/s；高程 6.39—51.03m；卫星数 0—34；航迹角有 189 个有效显示桶。筛选区间低频振动趋势 RMS 0.03450g、峰值偏差 0.78306g、峰峰值 1.02689g。

航向和航向标准差仍为空，这是设备处于卫导模式且未完成定向/组合导航的真实不可用状态。系统继续显示空值，不用 0 或推算值伪造曲线。10Hz 振动仍只用于 0—4Hz 载体趋势，不能作为高频机械故障频谱。

## 上线与数据完整性

- release：`/srv/ctmc-vehicle/releases/20260901-123434`（含后续报警阈值虚线 UI）；
- 回算前备份：`/srv/ctmc-vehicle/backups/motion-recovery-before-20260901-112302.sqlite`，约 801MB，`quick_check=ok`；
- 回算 26,726 条质量记录；回算前后原始采样均为 1,251,688 条，逻辑 SHA-256 均为 `c3c72e7b5cf8b6a1ea2bf078192031f03575bbb4451ae2b8566e6f2aaab63918`；
- 回算完成时待判定 0，60秒与600秒聚合均与原始点数一致且无无效桶，生产库 `quick_check=ok`；
- 本机与服务器均为 63 项 Python、14 项 Node 通过，跳过 0；
- 车载服务与留存定时器 active，`NRestarts=0`，healthz 正常；TCP 9000 接收器仍为 PID 1228，未中断，恢复期间的原始日志已追平入库。

## 正式页面验收

使用已登录的内置浏览器复验正式域名 `https://quality-ctmc.cloud/vehicle/`：24 小时视图加载 44,144 条采样，显示里程 6.30km、运动 11.1 分钟、最高速度 67.14km/h、时间断档 0；运行分段从 10:38:29.6 开始。展开“全部惯导信号”后共 14 个图表画布，12 组参数曲线和低频振动趋势均完成绘制，页面无横向溢出，浏览器控制台错误 0。

数据质量页显示“已自动恢复运动规则”，静止上下文截止 10:38:29.599，并记录系统于 10:38:39.600 根据完整车辆运动证据窗确认恢复。航向与航向标准差在 44,144 条样本中全部按不可用掩码处理，与卫导未定向状态一致。

## 回滚

若发现静止结束边界或新算法误判，停止车载服务与留存定时器但保持 TCP 9000 接收器，先保存当前新增原始数据，再基于上述备份恢复派生状态或创建新的不重叠静止上下文；应用可将 `current` 指回上一 release。不得直接用旧数据库覆盖恢复期间新收到的采样。
