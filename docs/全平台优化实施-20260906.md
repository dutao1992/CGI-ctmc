# 全平台优化实施记录

## 目标
按 9 月 6 日审计改善五模块响应与界面密度，优先保证数据刷新正确；保留已跑通的业务流程。
## 完成标准
车载非时间变化可刷新、401/403 明确且停止轮询；门户健康异常不锁入口；追溯精简首包并保持兼容；机组快照可缓存且详情按需；装配本地依赖及按需图表/Excel；失效检索与轮询减负。完成相关自动测试、构建、线上资源与只读交互验证，逐项记录未达项。
## 禁止事项
不改地速/振动判定、扫码主键、权限规则；不覆盖原有未提交修改，不删除业务数据或历史发布，不提交真实分析或测试绑定。
## 执行步骤
核对部署与可编译源；冻结本轮基线；车载正确性；门户和追溯数据路径；机组和装配布局/加载；失效列表；测试与构建；生成独立候选发布；切换、业务验证与回滚检查。
## 验收方法
使用项目已有 Python/Node 测试及构建；新增针对修订号、权限/站点隔离和延迟加载的行为测试。线上核对 release、资源指纹、服务健康、登录访问与业务只读结果。延迟只报告实际测得样本，不以工具耗时充当页面耗时。
## 风险
本地门户源码早于线上，需要对照部署文件避免回退；追溯首包兼容风险；缓存必须保持授权和业务更新可见。装配历史快照日期不改为当前日期。
## 回滚方案
发布前保留三个 current 指向及配置；平台/追溯新目录发布，仅复制白名单变更，业务数据目录不动。失败恢复旧 symlink 并重启受影响服务；不涉及算法重算，不运行全量车载历史重建。

## 进度
- 已核对线上三个发布指向与本地 Git 状态；取回平台及追溯部署源码至 analysis/platform-optimization-20260906，门户/失效可编译源码使用迁移仓库并对照线上。

## 发布结果

- 车载：`/srv/ctmc-vehicle/releases/20260906-140918`，服务与 healthz 正常。
- 平台：`/srv/ctmc-quality/releases/20260906-140744-optimization`，API/Web active。
- 追溯：`/srv/ctmc-quality/trace-releases/20260906-140840-optimization`，服务与 `/api/health` 正常。
- 全部旧 release 保留，可通过 current symlink 回滚。
- 装配脚本本地化、Worker 化未发布；需独立构建验证后再做。


## 回归补充（2026-09-06）

- 新增 `tests/test_platform_optimization.cjs`，覆盖车载修订号/会话失败、追溯紧凑首包、门户未知健康状态和机组快照可访问名称。
- Node 回归：50 passed；Python 回归：120 passed。
- 门户、失效分析生产构建通过，装配看板测试通过。
- 本轮未产生业务代码差异，线上继续使用上述已验证 release，无需重复切换。


## 运行总览简化（2026-09-06）

移除在线运行总览的“行程与停留”面板及对应渲染代码；离线解析页的区段证据保留。新增 `tests/test_overview_layout.cjs`。48 个 Node 测试、120 个 Python 测试通过。已部署至 `/srv/ctmc-vehicle/releases/20260906-143230`，healthz 正常，线上总览不含该模块。
